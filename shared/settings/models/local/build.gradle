plugins {
    id 'base.models.local'
}

android {
    namespace "${packageName}.shared.settings.models.local"
}

dependencies {
    // Shared modules
    implementation project(":shared:user:models:local")
}